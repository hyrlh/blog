# Project Agent Rules

- 全程使用中文与用户沟通，除非用户明确要求使用其他语言。
- 当上下文使用量达到或接近 80% 时，主动压缩上下文，并在继续任务前保留关键目标、已完成事项、未完成事项和必要文件路径。
- 只要能够更快、更稳地得到正确结果，就应主动并行调用工具、拆分任务并启用多个子代理协同执行；除非用户明确要求控制消耗，否则无需考虑 token 成本。
- 后续在本工程完成代码或内容修改并验证通过后，直接提交并 push 到当前分支；除非用户明确要求不要 push，或当前变更存在需要用户确认的风险。
